================================================================================
EMERGENT BEHAVIOR SOCIETY SIMULATOR (EBSS)
Project Status Report
Last verified: August 2026 (against commit b8e557e)
================================================================================

PROJECT OVERVIEW
---------------
A general-purpose AI platform for simulating societies of autonomous agents
that learn and adapt through behavioral evolution.


WHERE THE PROJECT STANDS
------------------------
The engine is built, not sketched. All four originally planned phases have
been implemented, and a default simulation now runs a society that feeds
itself, waters itself, shelters from the weather, and reproduces over tens of
thousands of ticks without intervention.

What remains is not "write the systems" but "connect what exists": several
analytics components are libraries nobody calls, agents still cannot see one
another or hear anything, and a handful of behaviours the design calls for
(agents making clothing, for one) have supporting code but nothing that
drives them.


SIZE AND ACTIVITY
-----------------
  Rust source files (src/)      181
  Lines in src/                 ~95,100
  Lines including tests,
    examples and plugins        ~102,200
  Commits                       272 (November 2025 - August 2026)
  Examples                      21
  Binaries                      3 (test_simulation, ebss_gui, ebss_bevy)

  Lines by module:
    agents/           27,512     analytics/        12,858
    world/            14,612     bevy_gui/          8,870
    environment/      12,643     core/              8,128
    gui/               7,638     visualization/     1,442
    bin/               1,279


BUILD STATUS
------------
  Every configuration builds.

  cargo build                          WORKS (15 warnings, all unused
                                       variables and imports)
  cargo build --features gui           WORKS (egui front end)
  cargo build --features bevy_gui      WORKS (Bevy front end)
  cargo check --workspace              WORKS (includes the plugin crate)
  cargo test --workspace               WORKS (see TEST STATUS)


TEST STATUS
-----------
  1,092 tests across the workspace:
  1,055 library tests
     15 integration tests (tests/environment_plugin_tests.rs)
     21 plugin tests (plugins/minecraft_survival)
      1 doc test

  All pass, with two known flaky tests that fail intermittently:
    world::tdd_tests::naturalistic_resource_tests::test_resource_clustering
    world::tdd_tests::spatial_planning_tests::test_minimize_travel_time_...

  Both seed World::new from thread_rng and assert on properties that a
  randomly generated world does not always satisfy. Measured failure rates
  of roughly 1-in-10 to 1-in-20 per run, present before any recent work.
  Fixing them means giving world generation a seed.


WHAT RUNS IN A LIVE SIMULATION
------------------------------
Verified as reached by Simulation::tick():

  Survival        hunger, thirst, energy, nutrition (energy, protein,
                  micronutrients), food spoilage, fatigue, body temperature,
                  exposure and shelter, injury and healing, death
  Lifecycle       aging, life stages, reproduction, pregnancy, birth,
                  nursing, inheritance of traits and behaviour
  Behaviour       drives, behaviour trees with weight learning, goals,
                  plans, foraging, drinking, sheltering, exploration
  Perception      smell of food and water within 25 tiles; sight of terrain,
                  resources and buildings within 10, negated by the Blind
                  trait; sharing of known resource locations between agents
  Social          proximity relationships, social interactions, gossip,
                  observational learning, shared knowledge, technology
                  discovery and spread
  World           terrain, climate, weather, seasons, resources and their
                  regeneration, buildings, crafting, fauna, flora, combat,
                  obstacle-aware pathfinding
  Persistence     save, load, autosave with checkpoint rotation
  Display         ASCII renderer, egui GUI (src/bin/gui.rs)


BUILT BUT NOT CONNECTED
-----------------------
These modules are implemented and tested in isolation, but nothing in the
simulation loop drives them. They are libraries awaiting a caller, not
missing features:

  analytics::web_api        ApiServer has zero call sites anywhere
  analytics::events         EventBus used only by its own tests
  analytics::replay         SessionRecorder used only by its own tests
  analytics::storage        StorageManager used only by its own tests
  analytics::metrics        \  work when a caller drives them; two examples
  analytics::emergence       > (ascii_simulation, phase4_analytics) show how,
  analytics::performance    /  but Simulation::tick does not feed them

  Zoning and territory      Building placement reads zone and territory
                            bonuses, but nothing outside tests ever creates a
                            zone or claims territory, so both are always empty

  Vision as a percept       Sight drives exploration and resource discovery,
                            but nothing populates vision.visible_agents, so
                            agents never see each other and
                            Percept::AgentDetected is never produced.

  Hearing                   Nothing feeds sounds from the world, so every
                            sound-derived percept is a dead path.


KNOWN GAPS IN BEHAVIOUR
-----------------------
  - Agents never make or wear clothing. Insulation is always zero, so they
    cycle between cold and shelter for their whole lives. Clothing recipes
    and an equipment system exist; nothing drives an agent to use them.
  - Agent fear is computed from unmet hunger, thirst and rest, so it sits
    high whenever an agent is between meals. Fleeing is gated on fear, which
    makes flight a common state rather than a response to danger.
  - Populations grow but have not been studied over very long runs. Nobody
    has characterised what happens past a few tens of thousands of ticks.


SUGGESTED NEXT STEPS
--------------------
  1. Seed world generation so the two flaky tests become deterministic and
     runs become reproducible.
  2. Decide the fate of the unconnected analytics: wire metrics, emergence
     detection and performance monitoring into Simulation::tick behind a
     config flag, or move them out of the crate.
  3. Give agents a reason to make clothing, so cold is something they solve
     rather than endure.
  4. Decide whether plugins/minecraft_survival should exist at all: it now
     compiles and its tests pass, but it duplicates
     src/environment/minecraft_survival.rs. Either merge what is unique to it
     (Perlin world generation, water structures) into the in-tree module, or
     keep it as the worked example of writing a plugin.
  5. Clean up the 15 build warnings and the committed
     src/analytics/mod.rs.backup.
  6. Characterise long-run behaviour: population dynamics, technology
     spread, and settlement patterns over 100k+ ticks.


HOW TO BUILD AND RUN
--------------------
  Prerequisites: Rust 1.70+, Cargo

  cargo build --release                 Build
  cargo test                            Run the test suite
  cargo run --bin test_simulation       Headless run with a status report
  cargo run --features gui --bin ebss_gui
                                        Interactive egui front end
  cargo run --example basic_survival    Smallest example
  cargo run --example ascii_simulation  Example that also drives the
                                        analytics modules

  cargo run --features bevy_gui --bin ebss_bevy
                                        Bevy front end


A NOTE ON THIS DOCUMENT
-----------------------
Every figure here was measured against the commit named at the top, not
estimated. If you are reading this well after that date, re-measure before
trusting it: the previous version of this file described a 12-file, 1,000
line project and was wrong by two orders of magnitude.

================================================================================
END OF STATUS REPORT
================================================================================
